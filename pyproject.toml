[project]
name = "zhimo-downloader"
version = "0.1.0"
description = "知末模型下载器"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.28.0",
    "playwright>=1.40.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
